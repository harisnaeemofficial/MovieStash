//============================= iScroll Setup =============================//

/*var myScroll;

function loaded() {
	myScroll = new IScroll('.iscroll-wrapper', {
		scrollbars: true,
		mouseWheel: true,
		interactiveScrollbars: true,
		shrinkScrollbars: 'scale',
		fadeScrollbars: true
	});
}

function loadedSelect() {
	myScroll = new IScroll('.iscroll-wrapper-select', {
		scrollbars: true,
		mouseWheel: true,
		interactiveScrollbars: true,
		shrinkScrollbars: 'scale'
	});
}

document.addEventListener('touchmove', function (e) { e.preventDefault(); }, false);*/